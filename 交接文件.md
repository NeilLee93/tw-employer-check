# 交接文件

最後更新：2026-09-14（第六輪）
狀態：**已進入實作。** 8 個資料集全數落地；歸戶主流程（名稱 → 統編 → 雇主名冊）
三個模組完成並通過全量驗收，45,311 個 org 收斂為 45,109 個雇主。判讀層
（v0.9.0）與改名候選偵測器（第 7 項）都已完成，全專案 102 個測試。
交付形態：**CLI 先行，核心邏輯抽成獨立 Python package**（已定案，2026-08-26）

下一步是「待辦」第 8、9 項：串接勞退與職安的判讀層、決定 CLI 介面長什麼樣。

---

## 一分鐘進入狀況

做一個本機工具，查台灣雇主的勞動法違規紀錄，並用**勞動法視角**判讀「這代表什麼」。

差異化不在「查得到」（勞動部官網和 g0v 都有），在三件事：
1. 法條白話化 + 嚴重度權重（需勞動法專業，是護城河）
2. 品牌名查得到（官方系統做不到）
3. 累犯與跨法規模式辨識（需要歷史，只有開放資料 API 有）

背景調查與資料源盤點見 `可行性調查.md`，那份是主文件。

---

## 已經確定的事實（不必重查）

| 項目 | 結論 |
|---|---|
| 資料可得性 | 勞動部 Open API，政府資料開放授權條款第 1 版，可商用 |
| 資料量 | 勞基法違規 77,813 筆 / 49,005 家 / 2011-11-15 ~ 2026-08-15 |
| 名稱比對難度 | **比預期低**。括號內 96.9% 是人名，基本正規化即可。已實作完成 |
| 罰鍰金額 | **2020 年以前全空**，金額分析起點必須設在 2021 |
| 法條對照表工作量 | 42 條主條文（細到條項 145 條），**前 8 條即涵蓋 87% 引用** |
| 有無現成工具 | 無。g0v 江明宗的 repo 仍在自動更新但資料源與我們相同；品牌對照無任何現成資料集 |
| 統一編號 | **拿得到，用財政部稅籍資料整份下載比對，79.4% 的事業單位補得上**。不必打經濟部 API |

---

## 今天最重要的發現：品牌對照有可靠來源

原本以為「品牌 → 法人名」只能靠人工知識。實測三家後發現不是：

> **求職平台的企業頁標題本身就是「品牌_法人名」格式。**

實測 3/3 全中：

| 法人名 | 品牌 | 證據 |
|---|---|---|
| 和德昌股份有限公司 | 麥當勞 | [104 企業頁](https://www.104.com.tw/company/5p9a8cg) 標題直接寫「麥當勞授權發展商_和德昌股份有限公司」；[維基百科](https://zh.wikipedia.org/zh-tw/%E5%92%8C%E5%BE%B7%E6%98%8C) |
| 台灣善商股份有限公司 | すき家 SUKIYA、はま寿司 | [台灣就業通](https://job.taiwanjobs.gov.tw/internet/jobwanted/company_desc2.aspx?EMPLOYER_ID=1194290) 標題寫「(SUKIYA / HAMASUSHI)」；[Cake](https://www.cake.me/companies/zensho-jp-jp) |
| 威合股份有限公司 | **無消費者品牌**（UEM EDGENTA 集團機構後勤外包） | [台灣就業通](https://job.taiwanjobs.gov.tw/internet/jobwanted/company_desc2.aspx?EMPLOYER_ID=4814) |

**方法論結論**：只要讀搜尋結果的**標題**就夠，不需要爬 104 的職缺內容，也就避開了先前擔心的服務條款問題。

派 agent 查剩下的是可行的，但必須加硬規則：
- 只認公司官網／求職平台企業頁／主流媒體／維基百科
- **禁止**用公司登記聚合站（twincn、findcompany、bznk 那類）當品牌依據——它們只有登記資料，agent 會被迫從「所營事業」推測
- 必須回傳證據 URL + 原文片段；查不到就回「找不到」，不准猜

---

## 第二個發現：法人改名會把品牌的歷史切斷

這比品牌別名更嚴重，因為它會**系統性低估**大企業：

| 品牌 | 被切成 | 證據 |
|---|---|---|
| 麥當勞 | 台灣麥當勞餐廳（3 筆, 201601~201605）+ 和德昌（19 筆, 201905~202607） | 2017 年經營權移轉並更名，**時間軸零重疊** |
| 台鐵 | 交通部台灣鐵路管理局（65）+ 國營台灣鐵路股份有限公司（10） | 公司化改制 |
| 富利 | 富利食品（5, 201507~201607）+ 富利餐飲（33, 201609~202607） | 時間軸接續無重疊，**疑為更名，待查證** |

**「時間軸無重疊 + 名稱高度相似」是可自動偵測的訊號**，值得寫成一個候選偵測器，不必人工翻。

---

## 第三個發現：kiang 的 repo 還活著，但沒有我們沒有的資料

2026-08-26 查證 `github.com/kiang/announcement.mol.gov.tw`：

| 項目 | 現況 |
|---|---|
| 是否停擺 | **沒有。** cron 每日 02:00 自動更新，最新 commit 2026-08-25 |
| 資料來源 | **與我們完全相同**的勞動部 Open API（`apiservice.mol.gov.tw/OdService/download/`） |
| 抓幾個資料集 | 6 個（勞基法／就服法／性平法／中高齡／職安／工會法），缺勞退與職災 |
| 歸戶邏輯 | 粗糙。`strpos('有限公司')` 取前綴、括號直接刪除，不處理臺／台與全形。**我們的做法比他好** |
| 儲存格式 | 一筆一個 JSON 檔（`data/年/主管機關/處分字號_名稱_法條.json`），archive 導向，對查詢無益 |

**結論：不需要改用他的資料源。** 但他的 repo 有兩件事對我們有價值：

1. **他的 fetch 腳本揭露了我們漏掉的資料坑**（HTML entity、null byte、欄位內換行）——見下方「已知資料坑」第 9、10 點，已驗證屬實。
2. **`docs/gcis.nat.g0v.tw/`**：他去打 g0v 商工登記 API 補統一編號。**該 API 實測仍活著**，`http://gcis.nat.g0v.tw/api/search/?q={公司名}` 回傳含 `統一編號` 欄位。抽驗 6 家精確命中 5 家（家福股份有限公司未命中，原因待查）。這是「無統編」這個最大工程風險的可能解方，值得評估。
3. 次要價值：他的 git 歷史從 2018 年起保留每日快照。若某筆裁處後來被勞動部撤下（行政救濟成功），單次快照看不到、他的歷史看得到。可作為「裁處撤銷偵測」的資料源。

---

## 第四個發現：其餘 7 個資料集只有 2 個值得認真做

2026-08-26 全數下載實測（`data/raw/`，不進版控）：

| 資料集 | 筆數 | 不重複事業單位 | 期間 | 金額欄位名 |
|---|---:|---:|---|---|
| 勞動基準法 | 77,813 | 49,005 | 20111115~20260815 | 罰鍰金額 |
| **勞工退休金條例** | **472,704** | **121,655** | 20191003~20260814 | 處分金額或滯納金 |
| **職業安全衛生法** | **82,600** | **38,286** | 20141007~20260815 | 罰鍰金額 |
| 勞工職業災害保險及保護法 | 25,497 | 20,213 | 20220810~20260814 | 處分金額或滯納金 |
| 性別平等工作法 | 1,338 | 1,225 | 20150512~20260815 | 罰鍰金額 |
| 就業服務法 | **89** | 89 | 20190905~20260811 | （無） |
| 工會法 | 34 | 21 | 20231005~20260727 | 罰鍰金額 |
| 中高齡者及高齡者就業促進法 | 12 | 11 | 20210805~20251110 | （無） |

**判讀：**

- 真正有量的只有 **勞退（472,704）** 與 **職安（82,600）**，兩者都比勞基法大或同級。原本待辦寫「串接其餘 7 個」是誤判工作量分布。
- **就服法只有 89 筆**。原本以為外籍移工／就業歧視是重點，中央這個資料集實際上形同空的。工會法 34 筆、中高齡 12 筆同理。這三個加起來 135 筆，串接價值極低。
- **勞退資料 97.6% 是同一條**：`勞工退休金條例第19條第1項`（461,234 筆），性質是**未按月提繳退休金的欠費與滯納金**，不是裁罰。
  > **設計硬規則：跨法規彙整時勞退不得與勞基法等權相加。** 否則所有排名會被欠費案件淹沒。
  > 實例：南山人壽在勞基法 208 筆已是全國第一，在勞退另有 239 + 106 筆，直接相加會嚴重扭曲。
- 職安法累犯前五全是營造業（國城營造 290、萬代福營造 225…），與勞基法的保全／客運集中完全不同產業。**兩者要分軌判讀，不能共用嚴重度模型。**

---

## 第五個發現：歸戶的關鍵不是清洗，是分類

實作名稱正規化時發現，剩下的難題都不是正則能解決的，得先做語意分類：

```
括號類：
  台灣超捷國際股份有限公司(畢揚賀(JamesEricBjornholt))   ← 巢狀，該剝
  彰化縣私立晨閔老人長期照顧中心(養護型)                    ← 機構名的一部分，不該剝
  台中市德水園身心障礙教養院(承辦單位:財團法人...)          ← 公辦民營關係，不該剝

串接類：
  玖盛公寓大廈管理維護有限公司劉國光    ← 負責人，該切
  中華航空股份有限公司企業工會          ← 獨立法人，不該併進中華航空
  友達光電股份有限公司台中廠            ← 同一法人的不同廠，該併
  笨蛋工作室有限公司                    ← 公司名本來就長這樣，不能動
```

**已定案的歸戶原則（使用者決定，2026-08-26）：**

| 型態 | 處理 | 理由 |
|---|---|---|
| 企業工會 | **不併入母公司**，以 `related_org` 標註關聯 | 依工會法是獨立法人，雇主是工會本身。求職者查中華航空不該看到工會的裁罰 |
| 廠區、分公司 | **併入母公司**，以 `site` 保留明細 | 同一法人，報告仍可拆出是哪一廠 |
| 附設機構（長照、補習班） | 同廠區處理 | 同屬一個法人的附設單位。此項為延伸推論，非使用者明示 |

**貫穿全模組的保守原則：分類不出來的殘餘一律留在 `org` 上。**
造成分裂還能靠別名表補，錯併救不回來。

實作與測試：`twec/names.py`、`tests/test_names.py`（35 個測試）、`scripts/audit_names.py`（全量驗收）。

---

## 第六個發現：統編補得上，而且來源不是經濟部，是財政部

待辦第 6 項原本寫「評估是否串接 gcis 補統一編號」。評估完的結論是**不要走 gcis**，
改用財政部的稅籍資料整份下載本機比對。理由與實測數字如下。

### 三條路實測比較

| 路線 | 取得方式 | 涵蓋範圍 | 判定 |
|---|---|---|---|
| **財政部 全國營業(稅籍)登記資料集** | 三個 CSV 整份下載，每日更新 | 389 萬列營業人，**公司＋商號＋分公司＋部分機構** | **採用** |
| 經濟部 GCIS 開放 API | 逐筆 HTTP 查詢，無全量下載 | 只有「公司」，不含商號、財團法人、政府機關 | 不採用，備用 |
| g0v `gcis.nat.g0v.tw` | 逐筆 HTTP 查詢 | 同上，且為非官方鏡像 | 不採用 |

使用者提到的 <https://findbiz.nat.gov.tw> 是經濟部商工登記公示資料的**查詢網頁**，
沒有自己的 API；它背後的開放資料在 <https://data.gcis.nat.gov.tw>，共 60 支 API，
規格見 <http://data.gcis.nat.gov.tw/resources/swagger/swagger.json>。

**GCIS 唯一能用「名稱查統編」的是「公司登記關鍵字查詢」** `6BBA2268-1367-4B42-9CCA-BC17499EBE8C`。
它有兩個坑，第一次試會以為 API 壞了：

- `$filter` **必須同時給 `Company_Name like X and Company_Status eq NN`**，
  只給名稱會回 HTTP 200 但 body 完全空白。狀態碼要 01～33 逐個試才涵蓋已解散公司。
- `like` 是子字串比對不是前綴，查「家福股份有限公司」會撈回「全家福」「佰家福」。

不採用 GCIS 的真正理由不是難用，是**涵蓋面**：目前補不上統編的 9,332 家裡，
公司型態只佔約 1,600 家，其餘是商號、財團法人、公立學校與政府機關，GCIS 根本沒有。
花數千次 API 呼叫換 3.6% 覆蓋率不划算。**GCIS 留作單筆補查用**（CLI 查到未命中時即時打一次）。

### 財政部稅籍資料

三個檔，都是政府資料開放授權條款第 1 版：

| dataset_id | 檔案 | 內容 | 列數 | 更新 |
|---|---|---|---:|---|
| 9400 | `BGMOPEN1.csv`（另有 .zip） | 營業中 | 171 萬 | 每日 |
| 75140 | `BGMOPEN1X.csv` | 停業 | 12 萬 | 每日 |
| 75141 | `BGMOPEN1Y.csv` | 非營業中（歇業、註銷、撤銷） | 206 萬 | 每月 |

欄位：`營業地址｜統一編號｜總機構統一編號｜營業人名稱｜資本額｜設立日期｜組織別名稱｜使用統一發票｜行業代號…`

> **`總機構統一編號` 是這份資料最值錢的欄位。** 分公司、營業所、門市全部靠它指回母公司，
> 比我們自己寫的名稱字串規則可靠得多。惠康百貨底下上百家分公司共用 `22662257`，
> 香港商世界健身全台分公司共用 `27940499`。

### 命中率（勞基法 77,813 列 / 45,311 家）

| | 家數 | 佔比 | 裁處列數 | 佔比 |
|---|---:|---:|---:|---:|
| 比對出統編 | 34,890 | 77.0% | 63,370 | 81.4% |
| 加上「同名只剩一家營業中」的挑選 | 35,979 | **79.4%** | 65,023 | **83.6%** |

拆到方法別（`twec/uniform_no.py` 的 `Resolution.method`）：

| 方法 | 家數 | 說明 |
|---|---:|---|
| `exact` | 28,700 | 名稱一字不差 |
| `natural_person` | 5,307 | 「王小明即某某商行」取「即」之後比對。**單一最大修補來源** |
| `sole_operating` | 1,089 | 同名多統編，但只剩一個營業中。信心較低，有 `high_confidence=False` 旗標 |
| `prefix_single` | 457 | 前綴命中單一統編，多為「XX有限公司台灣分公司」的外商 |
| `hq_via_prefix` | 414 | 前綴命中一群分公司，總機構統編一致 |
| `strip_juridical` | 12 | 剝掉「財團法人」等前綴，稅籍登記多半不帶 |
| `ambiguous_name` | 1,639 | 兩家同名都還在營業，不猜 |
| `ambiguous_prefix` | 79 | 前綴命中但統編發散，如「台北市立聯合醫院」各院區 |
| `miss` | 7,614 | 查無 |

### 補不上的 9,332 家是誰

不是資料源爛，是**這些雇主本來就沒有營業稅籍**：

- 政府機關與公立學校醫院：交通部台灣鐵路管理局（65 筆）、國立台灣大學、桃園市中壢區興國國民小學
- 聯合會計師事務所：安侯建業、勤業眾信本體無稅籍，只有它們投資的顧問公司有
- 財團法人附設機構：「財團法人○○基金會附設○○兒童之家」這種長名稱不會出現在稅籍
- 已改名或合併消滅的法人：中國人壽（已併入凱基人壽）、晶元光電（GCIS 與 g0v 兩邊都查無）

**這代表統編不是萬能鑰匙，名稱字串仍要留著當備援 key。** 兩者並存，不要拿統編取代 `org`。

### 意外收穫：統編自動抓出法人改名，兌現了待辦第 7 項的一大半

35,979 個有統編的名稱收斂為 35,747 個統編，其中 **230 組是「同一統編、不同名稱」**，
不需要相似度演算法就直接抓出來了：

| 統編 | 抓到的改名 | 對交接文件的影響 |
|---|---|---|
| `12411160` | 台灣麥當勞餐廳(3) ＝ 和德昌(19) | **推翻原判讀。** 原文寫「經營權移轉並更名、時間軸零重疊」當成兩家，統編相同表示是**同一法人更名**，22 筆該合併 |
| `22662257` | 惠康百貨(26) ＝ 統康生活(1) | 頂好被家樂福收購後改名 |
| `03550014` | 台灣富士全錄(32) ＝ 台灣富士軟片資訊(2) | |
| `22853565` | 來來超商(17) ＝ 富達零售(1) | |
| `97161500` | 富利餐飲(33) ＝ 怡和餐飲(3) | |
| `43211700` | 信東生技股份有限公司(20) ＝ 信東生技股份有限(1) | 連原始資料的漏字都抓出來 |

**同時推翻了另一個猜測**：富利食品（`23525767`）與富利餐飲（`97161500`）**統編不同，是兩家法人**，
原文「時間軸接續無重疊，疑為更名」的推測不成立。

統編解不了的仍有一類：**公司化改制是新設法人**。
交通部台灣鐵路管理局（政府機關，無統編）與國營台灣鐵路股份有限公司（`07524729`）
統編天生就不同，這種只能靠人工歸戶表。

實作與測試：`twec/uniform_no.py`、`tests/test_uniform_no.py`（21 個測試）、
`scripts/build_uniform_no.py`（全量比對並產出 `data/uniform_no_map.csv`）。

---

## 待辦（優先序，2026-08-26 第四輪修訂）

1. ~~查完剩下 11 家「待查」~~ **已完成**（使用者補齊 10 家，黑浮國際餐飲查無結果留白）
2. ~~串接其餘 7 個資料集~~ **資料層已完成**，7 份 CSV 已落地 `data/raw/`。判讀層尚未做
3. ~~決定交付形態~~ **已定案：CLI 先行，核心抽成 package**
4. ~~建法條 → 白話 + 嚴重度對照表~~ **改為漸進式，不再是阻塞點（2026-09-14 決定）**
   - 使用者判斷：官方描述本身已夠白話，逐條改寫價值不高——真正需要白話解讀的是
     「這代表什麼」，那要案例事實才生得出來，不是靠改寫條文。40 列條項層只填了 3 列
     白話、嚴重度全空，之後遇到真的看不懂再補，不強求填完
   - 因此判讀層改用 fallback 策略直接往下做：白話缺空時用**這一列自己的官方描述文字**；
     嚴重度缺空時用**罰鍰平均**（原型測過中位數在這份資料上鑑別力不夠——40 條項裡 27 條
     中位數都卡在法定最低罰鍰 20,000）自動切五級，不強制人工評分
   - **已實作**：`twec/interpret.py`（`load_law_table` / `build_severity_buckets` /
     `interpret_entity`），24 個行為規格見 `tests/test_interpret.py`。設計原型留在
     `spike/06_interpret_logic.py` + `spike/06_interpret_tui.py` 當原始出處
   - `data/勞基法白話對照表.xlsx` 沒填的欄位不是空白浪費，是 fallback 路徑本來就會走到的情況——
     之後隨時想補都可以補，補了就自動升級成人工白話/嚴重度，不用重新設計判讀層
5. ~~名稱正規化模組~~ **已完成**（`twec/names.py`，35 個測試，全量驗收通過）
   - 已處理坑 #2 無括號串接負責人、#3 巢狀括號、#9 HTML entity、#10 空白換行、#11 臺／台
   - 尚未處理：坑 #4 括號內是組織（147 列，目前保留在 org 上造成輕微分裂，不錯併）
   - 歸戶自 45,798 收斂為 45,311，較舊 spike 多合併 487 個名稱
   - **廠區與分公司已自動合併**，別名表「歸戶合併」頁的 World Gym 台灣分公司那一列可以移除了
6. ~~統編~~ **全部結案**（v0.6.0 補齊 + v0.7.0 接進主流程）
   - `twec/uniform_no.py`：勞基法 79.4% 的事業單位補得上統編（結論是用財政部稅籍，不走 gcis，見第六個發現）
   - `twec/roster.py`：統編已成為歸戶主 key，45,311 org → 45,109 個雇主
   - GCIS API 降級為備援：CLI 查到 `miss` 時再單筆補打
   - **合併規則刻意保守：只收 `high_confidence` 的統編**。因此更名組數是 202 而非原本說的 230，
     差的 28 組全是自然人商號換負責人（「林宗諺即泉威企業社」vs「泉威企業社」）。
     這 28 組**要不要合併是勞動法問題不是工程問題**：獨資商號的雇主是那個自然人，
     換負責人在稅籍上是同一主體、在勞動法上可能是換了雇主。等第 4 項一起決定
7. ~~寫改名候選偵測器~~ **已完成**（`twec/rename_candidates.py`，8 個測試，全專案 102 個）
   - 統編白送的 202 組改名不必再處理（`Roster.renamed` 直接可取）；這裡只吃殘餘——
     `Roster.entities` 裡 `key_kind == "name"`（兩邊都沒可信統編）的雇主
   - 訊號：時間軸不重疊＋名稱相似。比對前先用 `_core()` 去掉公司型態字尾
     （股份有限公司…），否則這種菜市場字尾會把毫不相干的公司配對在一起——
     實測「台灣善商」vs「好市多」不去字尾算出來的相似度，比真實改名案例
     （台灣麥當勞→和德昌）還高，這是不去字尾就會犯的錯
   - 名稱相似度用 stdlib `difflib.SequenceMatcher`，沒加新套件
   - **只能產候選，不能自動合併**：時間軸無重疊＋名稱相似不足以判定改名。
     富利食品與富利餐飲符合這個訊號，統編一查卻是兩家法人——這是設計上的
     硬限制，不是還沒做完
   - `scripts/build_rename_candidates.py` 產出 `data/改名候選.csv`。
     殘餘約 1 萬個雇主，兩兩比對 O(n²)，全量約需 10 分鐘，一次性批次工作，
     沒有優化到能常駐執行（例如 bigram 分桶索引），需要再說
8. **串接勞退與職安的判讀層**（資料已落地，但兩者都需要與勞基法不同的嚴重度模型）
9. 決定 CLI 介面長什麼樣（`twec` 目前只有 names 模組，還沒有進入點）

---

## 現在能跑什麼

```bash
pip install -e ".[dev]"
pytest                          # 102 個測試
python scripts/audit_names.py   # 全量 77,813 列驗收（需先下載原始資料）
python scripts/build_uniform_no.py  # 產出 data/uniform_no_map.csv
python scripts/build_roster.py      # 跑完整條歸戶主流程，產出 data/roster.csv
python scripts/build_rename_candidates.py  # 產出 data/改名候選.csv（全量約 10 分鐘）
```

```python
from twec.names import normalize, split_entity

split_entity(normalize("優志旺股份有限公司(渡&#37001;剛德)"))
# EntityName(org='優志旺股份有限公司', person='渡邉剛德', kind='company', related_org=None, site=None)

from twec.uniform_no import Registry
reg = Registry.from_dir("data/raw")          # 約 2 分鐘，一個 process 建一次
reg.resolve("和德昌股份有限公司").uniform_no  # '12411160'
reg.resolve("香港商世界健身事業有限公司")      # hq_via_prefix -> '27940499'
reg.resolve("交通部台灣鐵路管理局").uniform_no # None（政府機關無稅籍）

from twec.roster import Roster, count_orgs
roster = Roster.build(count_orgs("data/lsa_violations_raw.csv"), reg)
roster.of("台灣麥當勞餐廳股份有限公司").key    # '12411160'，與和德昌同一個 Entity
roster.of("交通部台灣鐵路管理局").key_kind      # 'name'，沒統編就用名稱當 key
len(roster.renamed)                            # 202 組統編指出的更名

from twec.rename_candidates import date_ranges, find_candidates
ranges = date_ranges("data/lsa_violations_raw.csv")
candidates = find_candidates(roster, ranges)   # 殘餘（無統編）雇主的改名候選，只能參考不能自動合併
```

`org` 是歸戶 key，下游全部掛在它上面。統編是加值欄位，不是取代 `org`。

**驗收基準（改動 `twec/names.py` 後應維持）：**

| 指標 | 值 |
|---|---:|
| 原始不重複名稱 | 49,758 |
| 正規化＋拆解後 | 45,311 |
| HTML entity 殘留 | 0 |
| 空白或換行殘留 | 0 |
| 誤判為人名的單位名 | 0 |

**驗收基準（改動 `twec/uniform_no.py` 後應維持，`python scripts/build_uniform_no.py`）：**

| 指標 | 值 |
|---|---:|
| 稅籍名冊列數 | 3,889,781 |
| 稅籍不重複名稱 | 3,441,705 |
| 比對出統編（家數） | 34,890（77.0%） |
| 含 `sole_operating`（家數） | 35,979（79.4%） |
| 含 `sole_operating`（裁處列） | 65,023（83.6%） |

> 稅籍檔每日更新，數字會隨下載日期小幅浮動；上表為 2026-08-26 下載的版本。
> 掉超過 1 個百分點才需要懷疑是不是改壞了。

**驗收基準（改動 `twec/roster.py` 後應維持，`python scripts/build_roster.py`）：**

| 指標 | 值 |
|---|---:|
| org（名稱層） | 45,311 |
| 歸戶後雇主數 | 45,109 |
| key 為統編 | 34,688（76.9%） |
| key 為名稱字串 | 10,421（其中 1,089 有低信心統編） |
| 統編併起來的更名組 | 202 組 / 404 個名稱 |

---

## 已知資料坑（實作時必須處理）

標 ✅ 者已由 `twec/names.py` 處理，其餘尚待處理。

1. ✅ **無統一編號欄位**，公司識別全靠名稱字串。
   已由 `twec/uniform_no.py` 用財政部稅籍資料補上 79.4%（見第六個發現）。
   **剩下的 20.6% 補不上是結構性的**（政府機關、公立學校、聯合會計師事務所無營業稅籍），
   不是可以再調參數解決的，名稱字串必須永遠留著當備援 key。
2. ✅ **負責人姓名有時沒有括號、直接串接**在公司名後面：
   - `台灣善商股份有限公司井上浩利`
   - `台南市立醫院(委託秀傳醫療社團法人經營)戴芳楟`
   → 舊 `strip_paren()` 抓不到，會多出假的獨立實體。
   已處理，但「括號在中間、尾巴再接人名」的第二例仍未處理（全資料集約 4 列）。
3. ✅ **巢狀括號** 589 筆（0.76%），如 `台灣超捷國際股份有限公司(畢揚賀(James Eric Bjornholt))`。
   改用反向掃描括號深度處理，不再用正則。
4. **括號內偶爾是組織**（147 筆，0.2%），需白名單。
   目前一律保留在 `org` 上——寧可分裂不可錯併。
5. **單筆多法條**：`違法法規法條` 用 `;` 分隔，20.5% 的列含多條，分析前須 explode。
6. **法規名稱不一致**：「勞基法」與「勞動基準法」混用，正規化後 227 → 139。
7. **裁處未確定亦公布**：`備註說明` 可見「行政救濟中」「訴願駁回」「訴願不受理」。
   **輸出必須標示狀態，不得逕稱該公司「違法」。** 這是硬規則，不是建議。
8. **罰鍰金額 2020 前全空**（見上表）。
9. ✅ **欄位含 HTML entity 未解碼**（2026-08-26 新增，由 kiang 腳本反推後實測驗證）：
   原始 CSV 內有 437 個數值 entity 與 68 個 `&nbsp;`，影響 423 列、347 家不重複事業單位。
   - `優志旺股份有限公司(渡&#37001;剛德)` → 應為 `渡邉剛德`
   - `&#32675;益企業有限公司(歐瑞生)` → 應為 `羣益企業有限公司`
   多為罕用字與異體字（邉、羣、峯、㶈）。原始 CSV 共 423 列受影響，
   其中 **198 列污染到公司名稱本體**（其餘落在括號內的負責人姓名，去括號後自然消失）。
   注意無效碼位（如 `&#2013268209;`）經 `html.unescape()` 會變成 U+FFFD，須一併移除。
10. ✅ **欄位內含換行與空白**：`事業單位名稱或負責人` 有 863 列含換行、1,308 列含空白
    （含全形空白 U+3000）、15 列頭尾有空白。例：`基隆汽車客運股份有限公司\n(呂奇峯)`。
    正規化須「移除所有空白字元」，不是只做 `strip()`。
    （註：舊 `spike/01` 的 `normalise()` 其實已處理此項，不是新發現的缺陷。）
11. ✅ **臺／台混用實測**：含「臺」1,428 列、含「台」4,090 列。統一轉「台」為必要步驟。
12. **職安法法條字串格式不同**：會用「暨」串接子法與母法，例
    `營造安全衛生設施標準第19條第1項暨職業安全衛生法第6條第1項`。
    勞基法的 `;` 分隔解析器**不能直接套用**，需另寫。
13. **據點與負責人兩層串接**，如 `新加坡商傲勝全球股份有限公司台灣分公司張查利`。
    `twec/names.py` 的 `_split_after_org_suffix` 只切一次：剝掉沒加括號的負責人之後，
    沒有再對剩下的字串剝一次據點，org 就停在「…台灣分公司」，與母公司分裂。
    全量 8 個名稱 / 8 列（2026-08-27 實測），v0.7.0 這批剛好都被統編救回來了。
    修法是切完之後遞迴再切一次，改完 `scripts/audit_names.py` 的收斂數會從 45,311 略降。
14. **法條與描述是兩串平行的 `;` 字串，且會對不上**：`違法法規法條` 與 `違反法規內容`
    以 `;` 分隔且理應一一對應，但 3,010 列（3.87%）數量對不上，無法確定哪句對哪條，只能整列跳過。
    另有兩個小坑：62 對的法條字串尾巴重複串接法規名稱（`勞動基準法第24條勞動基準法勞動基準法`）；
    官方描述的句尾標點時有時無，不先去掉會讓同一句被當成兩種句型（去掉後句型數 3,276 → 2,961）。

---

## 怎麼重跑

```bash
# 1. 重新下載原始資料（data/ 不進版控）
curl -o data/lsa_violations_raw.csv \
  https://apiservice.mol.gov.tw/OdService/download/A17000000J-030225-svj

# 1b. 其餘 7 個資料集（CSV 版 resource id，2026-08-26 實測可用）
#     A17000000J-030228-p2G  就業服務法
#     A17000000J-030226-sop  性別平等工作法
#     A17000000J-030227-POn  勞工退休金條例
#     A17000000J-030471-WCv  勞工職業災害保險及保護法
#     A17000000J-030472-iw5  中高齡者及高齡者就業促進法
#     A17000000J-030542-tEK  工會法
#     A17000000J-030466-h0a  職業安全衛生法
# 同一 dataset 底下有 CSV/JSON/XML 多個 resource，id 不同；
# 可用 https://apiservice.mol.gov.tw/OdService/rest/dataset/{dataset_id} 取回全部 resource id 再試。

# 1c. 財政部稅籍三檔（補統一編號用，約 380 MB 解開後）
curl -o data/raw/fia_BGMOPEN1.zip  https://eip.fia.gov.tw/data/BGMOPEN1.zip   # 營業中，解開後更名為 fia_BGMOPEN1.csv
curl -o data/raw/fia_BGMOPEN1X.csv https://eip.fia.gov.tw/data/BGMOPEN1X.csv  # 停業
curl -o data/raw/fia_BGMOPEN1Y.csv https://eip.fia.gov.tw/data/BGMOPEN1Y.csv  # 非營業中
# zip 內的檔名是 BGMOPEN1.csv，要改成 fia_BGMOPEN1.csv 才會被 Registry.from_dir 讀到

# 2. 正式模組
pip install -e ".[dev]"
pytest
python scripts/audit_names.py
python scripts/build_uniform_no.py
python scripts/build_roster.py               # 完整歸戶主流程 -> data/roster.csv
python scripts/build_law_workbook.py         # 第 4 項的填寫用 Excel（預設拒絕覆寫）

# 3. spike（已凍結，只在需要重建別名表時才跑）
python spike/03_brand_alias_shortlist.py     # 442 家分桶 -> data/brand_alias_shortlist.csv
python spike/04_build_alias_workbook.py      # -> data/品牌別名核對表.xlsx
python spike/05_triage_unknowns.py           # 待查公司縣市分診
```

Windows PowerShell 若中文亂碼：先 `$env:PYTHONIOENCODING='utf-8'`。

> ⚠️ **`spike/04` 會覆寫 `data/品牌別名核對表.xlsx`，使用者手工填的欄位會被蓋掉。**
> 重跑前先備份。

> `spike/` 全是**拋棄式**驗證腳本，已凍結不再維護，不要在上面疊功能。
> 唯一該保留的資產是 `04` 裡的 `ALIAS` 與 `MERGE_GROUPS` 兩張表——
> 其中 `MERGE_GROUPS` 的 World Gym 台灣分公司一列已被 `twec/names.py` 自動處理，可移除。
> `spike/01` 的 `normalise()` 與 `strip_paren()` 已被 `twec/names.py` 取代，勿再使用。

---

## 給下一個接手的人的提醒

- 品牌別名表的「待查」欄位是**刻意留白**的，不是漏填。別把空白當成「沒有別名」。
  使用者已於 2026-08-26 補齊 10 家，僅 `黑浮國際餐飲有限公司` 查無結果留白（疑已歇業）。
- 我曾把富利餐飲猜成漢堡王，是錯的（實為必勝客／肯德基，使用者指正）。這類猜測一律不要寫進表裡。
- `4_其餘未標註` 那 211 家我**沒有逐一檢視**，只是判斷多數名稱即可搜。要做完整還得再掃一輪。
- **別憑印象說某個資料坑存在，先跑數字。** 我曾把「欄位含空白換行」報成新 bug，
  實際上舊 spike 早就處理了；也曾說 HTML entity 影響 347 家，實際污染公司名的只有 176 家。
- 這個 repo 有東西在**自動 commit**（`chore: sync ...`）。它曾在 `.gitignore` 補上
  `data/raw/` 之前，就把 `data/raw/esa_就業服務法.csv` 與 `gea_性別平等工作法.csv`
  提交進版控，違反「原始資料不進版控」的慣例。要清掉可用 `git rm --cached`（檔案留本機）。
