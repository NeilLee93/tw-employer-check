# Changelog

所有重要變更都記錄於此。格式參考 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.0.0/)。

---

## [v0.8.0] — 2026-08-27

備好待辦第 4 項的填寫用表。程式只整理資料，白話化與嚴重度由人填。

### 新增
- 新增 `scripts/build_law_workbook.py`，產出 `data/勞基法白話對照表.xlsx`，四個分頁：
  - **條項層（必填）** 40 列：前 8 條的每個條項，附引用次數、句型數與罰鍰中位數／平均
  - **句型層（選填）** 2,961 列：官方描述去重後的實際句子，已標「必」48 句（各條前 80%）、
    「宜」142 句（到 90%），長尾可不理
  - **其餘條文（參考）** 30 列：本輪不做的條文，供第二輪選題
  - **說明**：填寫順序、嚴重度尺度草案、資料限制
- 嚴重度欄位加 Excel 資料驗證，限 1-5 整數，避免填出下游算不動的值
- 腳本**預設拒絕覆寫**已存在的 xlsx（要覆寫須加 `--force`）。
  `spike/04` 蓋掉手填別名表的教訓，這次先擋起來

### 修正
- 法條字串正規化補兩個坑：「勞基法」與「勞動基準法」混用、
  尾巴重複串接法規名稱（`勞動基準法第24條勞動基準法勞動基準法`，62 對）
- 官方描述去掉句尾標點再去重，句型數由 3,276 降為 2,961

### 已知限制
- `違法法規法條` 與 `違反法規內容` 是兩串平行的 `;` 分隔字串，
  數量對不上的列有 3,010 列（3.87%），無法確定哪句對哪條，整列跳過不猜

---

## [v0.7.0] — 2026-08-27

統編接進歸戶主流程。名稱正規化 → 統編 → 歸戶三段第一次串成一條完整的路。

### 新增
- 新增 `twec/roster.py`：把 `org` 收斂成「一個雇主一列」
  - `count_orgs(source)`：讀違規 CSV，正規化＋拆解後回傳 org → 裁處列數，是主流程入口
  - `Roster.build(counts, registry)`：用統編合併歸戶，回傳 `Entity(key, key_kind, uniform_no, names, count)`
  - `Roster.of(org)` 查一個名稱歸到哪個雇主；`Roster.renamed` 列出被統編併起來的更名組
  - `key_kind` 讓下游知道這一列的身分有多硬：`uniform_no`（76.9%）或 `name`（備援）
- 新增 `tests/test_roster.py`，14 個測試（全專案 70 個）
- 新增 `scripts/build_roster.py`，跑全量並產出 `data/roster.csv`

### 變更
- **待辦第 6 項完全結案**。統編不再只是躺在對照表裡的加值欄位，已成為歸戶的主 key
- 合併規則定案為**只收 `high_confidence` 的統編**。`sole_operating` 的統編仍記錄，但不合併：
  它取的是「現在還營業的那家」，用來合併會把換過負責人的獨資商號錯併成同一個雇主。
  依此規則，統編白送的更名組數由 230 收斂為 **202 組（涵蓋 404 個名稱）**
- `scripts/build_uniform_no.py` 改用 `twec.roster.count_orgs`，移除重複的計數邏輯

### 已知限制
- 歸戶後仍有 10,421 家以名稱字串為 key，其中 1,089 家有低信心統編（結構性補不上，非參數問題）
- 新發現一個名稱坑（坑 #13）：`新加坡商傲勝全球股份有限公司台灣分公司張查利` 這種
  **「分公司 ＋ 無括號負責人」兩層串接**，`twec/names.py` 只切一次，剝掉負責人之後
  沒有再剝一次據點，org 就停在「…台灣分公司」。全量共 8 個名稱 / 8 列。
  這次剛好被統編救回來，沒有統編的同型名稱仍會分裂。修法是遞迴再切一次，屬待辦第 5 項

---

## [v0.6.0] — 2026-08-26

補上統一編號。原始資料沒有這個欄位，是專案最大的工程風險，現已解掉八成。

### 新增
- 新增 `twec/uniform_no.py`：用財政部「全國營業(稅籍)登記資料集」替事業單位名稱補統一編號
  - `Registry.from_dir(raw_dir)`：讀營業中／停業／非營業中三個 CSV，共 389 萬列
  - `Registry.resolve(org)`：回傳 `Resolution(uniform_no, method, candidates)`，
    附 `resolved` 與 `high_confidence` 兩個旗標
  - 比對階梯共 6 條路徑：`exact`、`natural_person`（「王小明即某某商行」取「即」之後）、
    `hq_via_prefix`（分公司經總機構統編指回母公司）、`prefix_single`（外商在台分公司）、
    `strip_juridical`（剝「財團法人」等前綴）、`sole_operating`（同名多統編但只剩一家營業中）
- 新增 `tests/test_uniform_no.py`，21 個測試
- 新增 `scripts/build_uniform_no.py`，全量比對並產出 `data/uniform_no_map.csv`
- 交接文件新增「第六個發現：統編補得上，而且來源不是經濟部，是財政部」

### 修正
- **修正麥當勞歸戶判讀錯誤**：原判「台灣麥當勞餐廳」與「和德昌」是經營權移轉後的兩家公司，
  統編一查同為 `12411160`，是**同一法人更名**，22 筆裁處應合併計算
- **修正富利改名的錯誤推測**：原判「富利食品與富利餐飲時間軸接續無重疊，疑為更名」，
  統編分別為 `23525767` 與 `97161500`，是兩家法人，推測不成立

### 變更
- 待辦第 6 項結案，結論是**不串接經濟部 GCIS API**，改用財政部稅籍資料整份下載本機比對。
  理由是涵蓋面：補不上的 9,332 家裡公司型態只佔約 1,600 家，其餘是商號、財團法人、
  公立學校與政府機關，GCIS 根本沒有收錄。GCIS 降級為 CLI 單筆補查的備援
- 待辦第 7 項（改名候選偵測器）範圍縮小：統編已直接給出 230 組確定的改名，
  相似度演算法只需處理兩邊都沒統編的殘餘

### 已知限制
- 補不上統編的 20.6%（9,332 家）是結構性的：政府機關、公立學校醫院、
  聯合會計師事務所本體無營業稅籍。**名稱字串必須永遠留著當備援 key，統編不能取代 `org`**
- `sole_operating`（1,089 家）是唯一有取捨的路徑，取的是「現在還營業的那家」，
  未必是裁處當時的那家。要嚴格請只收 `high_confidence` 的結果
- 公司化改制屬新設法人，統編天生不同（台鐵、中華郵政這類），統編解不了，仍需人工歸戶表
- 統編尚未接進歸戶主流程，目前只產出對照表

---

## [v0.5.0] — 2026-08-26

首次有正式模組。專案自 spike 階段進入實作。

### 新增
- 新增 `twec/` 套件與 `twec/names.py`：事業單位名稱正規化與拆解，是整個專案的歸戶地基
  - `normalize(raw)`：HTML entity 解碼 → NFKC → 臺轉台 → 移除所有空白，四步順序不可調換
  - `split_entity(name)`：回傳 `EntityName(org, person, kind, related_org, site)`
- 新增 `tests/test_names.py`，35 個測試，以 TDD 逐一驅動實作
- 新增 `scripts/audit_names.py`，拿全量 77,813 列驗收，補單元測試涵蓋不到的範圍
- 新增 `pyproject.toml`

### 修正
- **修正 HTML entity 未解碼**：198 列公司名稱本體含未解碼 entity（`&#32675;益企業有限公司` 應為「羣益」），
  這些公司原本永遠歸不了戶。修正後殘留 0 列。
  無效碼位（如 `&#2013268209;`）經 `html.unescape` 會變成 U+FFFD，一併移除
- **修正巢狀括號剝不掉**：舊 `strip_paren` 的正則處理不了
  `台灣超捷國際股份有限公司(畢揚賀(JamesEricBjornholt))`，改為由字尾反向掃描括號深度
- **修正負責人姓名未加括號直接串接**：`漢程汽車客運股份有限公司楊豐文` 原本會因換負責人而裂成多個實體
- 廠區與分公司自動歸戶到母公司：`台積電先進封測五廠`、`香港商世界健身事業有限公司台灣分公司`、
  全聯 5 個分公司、台灣中油 4 個事業部營業處等，原需人工維護的合併現已自動化
- 歸戶收斂自 45,798 降為 45,311，較舊 spike 多合併 487 個名稱，抽查前 14 大群組無錯併

### 變更
- 確立歸戶原則（經使用者決定）：
  - **企業工會不併入母公司**——依工會法是獨立法人，雇主是工會本身；改以 `related_org` 標註關聯
  - **廠區與分公司併入母公司**——同一法人，以 `site` 保留明細供報告拆分
- 確立保守原則：分類不出來的殘餘一律留在 org 上。造成分裂還能靠別名表補，錯併救不回來
- `spike/` 標記為凍結，不再維護
- README 改寫「快速啟動」與「目錄結構」，加入模組用法範例

### 已知限制
- 括號內是營運據點者未特別處理（如 `台灣電力股份有限公司(嘉義區營業處)`），全資料集 2 列
- 括號在名稱中間、尾巴再接負責人者未處理（如 `台南市立醫院(委託秀傳醫療社團法人經營)戴芳楟`），約 4 列
- 兩者合計 6 列 / 77,813，暫不值得為此增加複雜度

---

## [v0.4.0] — 2026-08-26

### 新增
- 下載其餘 7 個勞動部資料集全量 CSV 至 `data/raw/`（勞退 472,704 筆、職安 82,600 筆、職災 25,497 筆、性平法 1,338 筆、就服法 89 筆、工會法 34 筆、中高齡 12 筆），並記錄各資料集的 CSV resource id
- 交接文件新增「第三個發現：kiang 的 repo 還活著，但沒有我們沒有的資料」與「第四個發現：其餘 7 個資料集只有 2 個值得認真做」兩節
- 已知資料坑自 8 項增為 12 項，新增 HTML entity 未解碼、欄位內換行與空白、臺／台混用實測數、職安法「暨」串接法條格式

### 修正
- 品牌別名核對表補齊：使用者填入 10 家原「待查」公司（宏華＝中華電信子公司、美德耐＝維康醫療用品、笑笑笑＝小北百貨、豐隆大飯店＝台北君悅、樹籽＝南方莊園、天廬育樂＝日月潭大淶閣等）；黑浮國際餐飲查無結果，刻意留白

### 變更
- 交付形態定案：CLI 先行，核心邏輯抽成獨立 Python package，輸出層可替換
- 修正「g0v 江明宗專案已停擺」的判斷。實測其 repo cron 每日 02:00 仍在自動更新（最新 commit 2026-08-25），但資料源與我們相同、歸戶邏輯較粗糙，不需改用
- 修正待辦第 5 項「串接其餘 7 個資料集」的工作量估計。實際只有勞退與職安有量，就服法／工會法／中高齡三者合計僅 135 筆
- 新增設計硬規則：勞退資料 97.6% 為「第19條第1項未按月提繳」的欠費與滯納金，跨法規彙整時不得與勞基法裁罰等權相加
- 法條對照表範圍收斂：勞基法 42 條中前 8 條即涵蓋 87% 引用，優先只做這 8 條

---

## [v0.3.0] — 2026-08-26

### 新增
- 新增 `交接文件.md`，供下次接手時冷啟動
- 新增 `spike/05_triage_unknowns.py`，用縣市分布替待查公司分診（跨 3 縣市以上才可能是連鎖），20 家縮為 14 家
- 新增 `.gitignore`，原始 CSV 不進版控
- 別名表新增：富利餐飲＝必勝客／肯德基、台灣善商＝すき家／はま寿司、和德昌＝麥當勞、威合＝無消費者品牌
- 歸戶合併清單自 2 組增為 5 組，新增麥當勞（高）、富利（中）、威合威務（中），並加註信心欄

### 修正
- 修正富利餐飲的錯誤推測（原標「疑為漢堡王」，實為必勝客／肯德基，由使用者指正）

### 變更
- 確認品牌對照有可靠取得方式：求職平台企業頁標題即「品牌_法人名」格式，實測 3/3 命中，且只需讀標題、不需爬職缺內容
- 別名表信心分布更新為 高 59／中 11／待查 11

---

## [v0.2.0] — 2026-08-26

### 新增
- 新增 `spike/03_brand_alias_shortlist.py`，將違規 10 次以上的 442 家事業單位自動分桶，排除 129 家不需別名者
- 新增 `spike/04_build_alias_workbook.py`，產出 `data/品牌別名核對表.xlsx` 供人工核對
- 新增品牌別名建議表（81 家，含信心等級：高 55／中 11／待查 15）
- 新增「客運插字規則」自動化處理（21 家，如「國光汽車客運」→「國光客運」），此類不需人工建表
- 新增「歸戶合併」清單（台鐵改制前後、World Gym 總分公司）

### 變更
- 確認品牌對照無現成工具可用。智財局商標公報開放資料雖含商標權人欄位，但權利人多為境外母公司而非台灣僱用法人，方向不符需求；BrandPedia 僅收錄 60 個品牌且無 API；商工登記無品牌欄位

---

## [v0.1.0] — 2026-08-26

專案啟動，完成可行性驗證。尚無可用功能。

### 新增
- 新增 `可行性調查.md`，包含資料源盤點（8 個勞動部開放資料集）、前案調查（勞動部官方系統、各縣市系統、g0v 江明宗專案）與實測結果
- 新增 `spike/01_name_matching_spike.py`，驗證「事業單位名稱或負責人」欄位的可比對性
- 新增 `spike/02_followups.py`，追查罰鍰金額缺值、法條字串正規化、品牌名與法人名落差三項疑點
- 新增 `README.md` 與 `CHANGELOG.md`
- 下載勞動基準法違規資料全量 CSV（77,813 筆，公告日期 2011-11-15 ~ 2026-08-15）

### 變更
- 修正原先「名稱比對是主要工程風險」的推測。實測顯示括號內容 96.9% 為人名、僅 0.2% 為組織名，基本正規化即足以應付；主要風險改判為「品牌名 ≠ 法人名」（實測「家樂福」3 筆 vs「家福」143 筆）與「罰鍰金額 2020 年以前全空」
